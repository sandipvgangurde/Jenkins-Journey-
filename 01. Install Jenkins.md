# 🧰 **SOP: Setting Up Jenkins Using Docker**

---

## 🏗️ **1. Introduction**

**Jenkins** is an open-source automation server used for **Continuous Integration (CI)** and **Continuous Deployment (CD)** 🚀.
It helps developers automate repetitive tasks like **building**, **testing**, and **deploying** code.

Running Jenkins in **Docker 🐳** provides an easy, clean, and isolated setup — no need to manually install Java or dependencies on your local machine.

### 💡 **Why Jenkins + Docker?**

* 🧩 No Java installation required (Docker image includes it)
* ⚙️ Easy setup & cleanup
* 🔄 Portable & consistent across environments
* 🧱 Great for DevOps and CI/CD automation

---

## ⚙️ **2. Prerequisites**

Before you start, ensure you have the following ✅:

| Requirement                          | Description                                                 |
| ------------------------------------ | ----------------------------------------------------------- |
| 🐳 **Docker Installed**              | Jenkins runs as a Docker container.                         |
| 💻 **Terminal/SSH Access**           | Needed to run Docker commands.                              |
| 🌐 **Web Browser**                   | To access the Jenkins web UI (e.g., Chrome, Edge, Firefox). |
| 🔑 **Basic Linux Command Knowledge** | Helpful, but not mandatory.                                 |

👉 If you don’t have Docker installed, visit [Docker’s Official Installation Guide](https://docs.docker.com/get-docker/).

---

## 🚀 **3. Setup Steps Overview**

| 🪜 Step | 📝 Description            | 💻 Command                                                                     |
| ------- | ------------------------- | ------------------------------------------------------------------------------ |
| 1️⃣     | Pull Jenkins Docker Image | `docker pull jenkins/jenkins:lts`                                              |
| 2️⃣     | Run Jenkins Container     | `docker run -d -p 8080:8080 -p 50000:50000 --name jenkins jenkins/jenkins:lts` |
| 3️⃣     | Retrieve Admin Password   | `docker exec jenkins cat /var/jenkins_home/secrets/initialAdminPassword`       |
| 4️⃣     | Access Jenkins Web UI     | Open `http://localhost:8080`                                                   |
| 5️⃣     | Unlock Jenkins            | Paste password and continue setup                                              |
| 6️⃣     | Install Suggested Plugins | Choose “Install suggested plugins”                                             |
| 7️⃣     | Create Admin User         | Add username, password, and email                                              |
| 8️⃣     | Jenkins is Ready 🎉       | Start creating jobs & pipelines                                                |

---

## 🧱 **Step-by-Step Jenkins Setup**

---

### 🧩 **Step 1: Pull Jenkins Image**

Pull the latest **Long-Term Support (LTS)** version of Jenkins:

```bash
docker pull jenkins/jenkins:lts
```

📘 **Explanation:**
This command downloads the official Jenkins image containing Jenkins + Java (no separate Java install needed 🎯).

🧠 **Real-World Analogy:**
It’s like downloading a pre-configured virtual machine with Jenkins preinstalled.

---

### 🧰 **Step 2: Run Jenkins Container**

Run Jenkins inside a Docker container with this command:

```bash
docker run -d -p 8080:8080 -p 50000:50000 --name jenkins jenkins/jenkins:lts
```

📘 **Explanation:**

* `-d` → Runs Jenkins in background
* `-p 8080:8080` → Maps Jenkins web port
* `-p 50000:50000` → Used for Jenkins agents
* `--name jenkins` → Container name for easy management

🌍 **Access Jenkins** at [http://localhost:8080](http://localhost:8080)

🧠 **Analogy:**
Think of this like turning on a server that’s ready to accept requests.

---

### 🔑 **Step 3: Retrieve the Initial Admin Password**

To unlock Jenkins, fetch the initial admin password using:

```bash
docker exec jenkins cat /var/jenkins_home/secrets/initialAdminPassword
```

🔍 **This will output something like:**

```
d7f2a8d8e5c441f5b715bd9f5c10c7b0
```

💡 Copy this password — you’ll need it to unlock Jenkins.

---

### 🖥️ **Step 4: Access Jenkins Web Interface**

Open your browser and go to:

```
http://localhost:8080
```

If you’re running Jenkins on a remote server:

```
http://<your-server-ip>:8080
```

Paste the **admin password** you copied earlier 🔐.

---

### ⚙️ **Step 5: Install Plugins**

Select **“Install suggested plugins”**.
These are the most common plugins you’ll need (Git, Pipeline, etc.) ✅

🧠 **Pro Tip:**
Later, you can add more plugins from **Manage Jenkins → Manage Plugins**.

---

### 👤 **Step 6: Create Admin User**

Set up your **admin user account**:

* 👤 Username: `admin`
* 🔒 Password: `yourpassword`
* 📧 Email: `you@example.com`

Then click **Save and Continue**.

---

### 🎯 **Step 7: Jenkins is Ready!**

Once complete, Jenkins will redirect you to the main dashboard 🏁
You’re now ready to create **Freestyle Projects** or **Pipeline Jobs**! 💥

📍 Access again anytime:

```
http://localhost:8080
```

---

## 🧩 **4. Common Docker Commands for Jenkins**

| Command                     | Purpose                                  |
| --------------------------- | ---------------------------------------- |
| 🟢 `docker start jenkins`   | Start Jenkins container                  |
| 🔴 `docker stop jenkins`    | Stop Jenkins container                   |
| ♻️ `docker restart jenkins` | Restart Jenkins                          |
| ❌ `docker rm -f jenkins`    | Remove Jenkins container                 |
| 🔎 `docker ps -a`           | View all containers (running or stopped) |

---

## 🛠️ **5. Troubleshooting Tips**

| ⚠️ Problem              | 💡 Solution                                          |
| ----------------------- | ---------------------------------------------------- |
| Jenkins not loading     | Ensure container is running (`docker ps`)            |
| Port 8080 in use        | Change mapping: `-p 9090:8080`                       |
| Plugins fail to install | Check network connectivity                           |
| Forgot password         | Restart Jenkins and view logs for reset instructions |

---

## 🌳 **6. Jenkins Workflow Tree (Visual Guide)**

Here’s how Jenkins fits in a real DevOps pipeline 🌐

```
🌳 Jenkins (Automation Server)
│
├── 🧩 Source Code Management
│    └── GitHub / GitLab / Bitbucket
│
├── 🏗️ Build Stage
│    ├── Compile code
│    ├── Run unit tests
│    └── Package application
│
├── 🧪 Test Stage
│    ├── Integration testing
│    └── Code quality checks
│
├── 🚀 Deploy Stage
│    ├── Push to staging/production
│    └── Notify team (Slack/Email)
│
└── 📊 Monitor Stage
     ├── Track deployments
     └── Feedback to developers
```

💬 **Hello Jenkins Program!**
When you’re done setting up, your first pipeline can be as simple as:

```groovy
pipeline {
    agent any
    stages {
        stage('Hello') {
            steps {
                echo '👋 Hello, Jenkins World!'
            }
        }
    }
}
```

🧠 **Result:**
In your Jenkins console output, you’ll see:

```
[Pipeline] echo
👋 Hello, Jenkins World!
```

---

## 🏁 **7. Summary**

| ✅ Step | 🧭 Description                            |
| ------ | ----------------------------------------- |
| 1️⃣    | Installed Jenkins in Docker               |
| 2️⃣    | Retrieved admin password                  |
| 3️⃣    | Unlocked Jenkins & installed plugins      |
| 4️⃣    | Created admin user                        |
| 5️⃣    | Accessed Jenkins Dashboard                |
| 6️⃣    | Created your first "Hello Jenkins" job 👋 |

---

🎉 **Congratulations! You’ve successfully deployed Jenkins via Docker!**
You’re now ready to automate builds, tests, and deployments — the DevOps way 🔄💻.

---
